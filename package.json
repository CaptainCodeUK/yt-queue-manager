{
  "name": "yt-queue-manager",
  "type": "module",
  "version": "1.2.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "release": "npm run build && rm -rf .release && mkdir -p .release/yt-queue-manager && cp -R dist/. .release/yt-queue-manager/ && (cd .release && zip -qrFS ../releases/yt-queue-manager-v$npm_package_version.zip yt-queue-manager)",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.31",
    "@types/chrome": "^0.0.283",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^4.1.11"
  }
}
